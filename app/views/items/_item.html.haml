.content
  %p= item.name
  %p
    = item.price
    円
    -# エラーが出るので後の実装までコメントアウトします
    -# = item.condition.name
    -# = item.postagepayer.name
    -# = item.postagetype.name
    -# = item.preparationdays.name
    = link_to '編集', edit_item_path(item)

  - item.images.each do |image|
    = image_tag image.src.url
